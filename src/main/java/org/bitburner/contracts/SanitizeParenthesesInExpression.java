package org.bitburner.contracts;

public class SanitizeParenthesesInExpression {
    public static int getMinimumSanitizedExpression(String expression) {
        boolean isOpenExists;
//        if(isOpenExists && !expression.contains(")")) expression = expression.substring(1);
        return 0;
    }
}
